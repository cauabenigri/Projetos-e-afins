#include <stdio.h>
#include <stdlib.h>
void main(){
    int num, i, r = 1;
    printf("Digite um número para fazer o fatorial: ");
    scanf("%d", &num);
    for(num; num > 0; num--){
        r *= ("%d", num);
}    printf("%d", r);

}
